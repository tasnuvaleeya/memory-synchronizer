# Node
node_modules/
npm-debug.log*

features/
# Build output
dist/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest-cache/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Local runtime cache (used when agentsync runs against a real repo)
.agentctx/cache/
.agentctx/last-sync.json

# GitHub Actions need their dist committed (they have no install step)
!packages/action/dist/
