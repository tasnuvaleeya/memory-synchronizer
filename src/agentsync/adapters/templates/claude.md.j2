{% from "_section.md.j2" import section %}
# {{ project_name }} — Agent Context

{% if project_description %}
{{ project_description }}

{% endif %}
> This file is generated by `agentsync` from `agent/`. Edit the source files,
> then run `agentsync sync`.

{% for entry in entries %}
{{ section(entry) }}

{% endfor %}
{% if dropped %}
---

_The following sections were omitted by the adapter's token budget:_
{% for entry in dropped %}
- `agent/{{ entry.rel_path }}` — {{ entry.frontmatter.description if entry.frontmatter else entry.name }}
{% endfor %}
{% endif %}
